/// <reference types="node" />
import { SpawnOptions } from "child_process";
import { ExecOptions, ExtraSpawnOptions } from "builder-util";
import { VmManager } from "./vm";
export declare class MonoVmManager extends VmManager {
    private readonly currentDirectory;
    constructor(currentDirectory: string);
    exec(file: string, args: Array<string>, options?: ExecOptions, isLogOutIfDebug?: boolean): Promise<string>;
    spawn(file: string, args: Array<string>, options?: SpawnOptions, extraOptions?: ExtraSpawnOptions): Promise<any>;
    toVmFile(file: string): string;
}
